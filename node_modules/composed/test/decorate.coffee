#!/usr/bin/env coffee

assert = require 'assert'

composer = require '../lib/composer'

wait = require('wait')()


a = (req, res, next) ->
    next()
mw = [a]
initial = composer mw
req = res = null

initial req, res, -> wait()
